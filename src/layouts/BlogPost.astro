---
import type { CollectionEntry } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import FormattedDate from '../components/FormattedDate.astro';
import Header from '../components/Header.astro';

type Props = CollectionEntry<'blog'>['data'];

const { title, description, pubDate, updatedDate } = Astro.props;
---

<html lang="en">
	<head>
		<BaseHead title={title} description={description} />
	</head>

	<body class="bg-stone-100 font-serif text-stone-800">
		<Header />
		<main class="max-w-3xl mx-auto px-4 py-8">
			<article>
				<header class="mb-8 text-center">
					<div class="mb-4">
						<FormattedDate date={pubDate} />
						{
							updatedDate && (
								<div class="font-mono text-sm text-stone-500 uppercase mt-1">
									Last updated on <FormattedDate date={updatedDate} />
								</div>
							)
						}
					</div>
					<h1 class="font-serif text-4xl md:text-5xl font-bold text-stone-900 mb-6">{title}</h1>
					<hr class="border-stone-300 max-w-xs mx-auto" />
				</header>
				<div class="prose prose-lg max-w-none">
					<slot />
				</div>
			</article>
		</main>
	</body>
</html>
